// This file was generated by prisma-field-encryption.

import type { PrismaClient, entidades_documento } from '@prisma/client'
import {
  ProgressReportCallback,
  defaultProgressReport,
  visitRecords
} from 'prisma-field-encryption/dist/generator/runtime'

type Cursor = entidades_documento['id']

export async function migrate(
  client: PrismaClient,
  reportProgress: ProgressReportCallback = defaultProgressReport
): Promise<number> {
  return visitRecords<PrismaClient, Cursor>({
    modelName: 'entidades_documento',
    client,
    getTotalCount: client.entidades_documento.count,
    migrateRecord,
    reportProgress,
  })
}

async function migrateRecord(client: PrismaClient, cursor: Cursor | undefined) {
  return await client.$transaction(async tx => {
    const record = await tx.entidades_documento.findFirst({
      take: 1,
      skip: cursor === undefined ? undefined : 1,
      ...(cursor === undefined
        ? {}
        : {
            cursor: {
              id: cursor
            }
          }),
      orderBy: {
        id: 'asc'
      },
      select: {
        id: true,
        nombre: true,
        direccion: true,
        identificador_fiscal: true,
        telefono: true,
        email: true
      }
    })
    if (!record) {
      return cursor
    }
    await tx.entidades_documento.update({
      where: {
        id: record.id
      },
      data: {
        nombre: record.nombre,
        direccion: record.direccion,
        identificador_fiscal: record.identificador_fiscal,
        telefono: record.telefono,
        email: record.email
      }
    })
    return record.id
  })
}

/**
 * Internal model:
 * {
 *   "cursor": "id",
 *   "fields": {
 *     "nombre": {
 *       "encrypt": true,
 *       "strictDecryption": false
 *     },
 *     "direccion": {
 *       "encrypt": true,
 *       "strictDecryption": false
 *     },
 *     "identificador_fiscal": {
 *       "encrypt": true,
 *       "strictDecryption": false
 *     },
 *     "telefono": {
 *       "encrypt": true,
 *       "strictDecryption": false
 *     },
 *     "email": {
 *       "encrypt": true,
 *       "strictDecryption": false
 *     }
 *   },
 *   "connections": {
 *     "documentos": {
 *       "modelName": "documentos",
 *       "isList": false
 *     },
 *     "empresas": {
 *       "modelName": "empresas",
 *       "isList": false
 *     }
 *   }
 * }
 */
